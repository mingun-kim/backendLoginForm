// const pg = require("pg");
// const client = pg.Client;

const pgInit = {
    user: "ubuntu",
    host: "localhost",
    database: "testdb",
    password: "1234",
    port: 5432
}

module.exports = pgInit;